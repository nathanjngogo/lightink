# LightInk 轻墨 v1.0.10

修复更新。

## 修复

- **欢迎页「打开文件夹」无效**——选完目录后书架没有任何变化
  - 根因：该按钮还在调用旧版单目录书架接口，扫描结果被直接丢弃，等于白选
  - 修复：选目录后自动新建一个**以目录名命名的书架文件夹**，并把目录下所有 `.md` / `.markdown` / `.txt` 文档导入其中，完成后提示导入数量
  - 导入的文档自动持久化，重启后书架保留

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.10.exe` | Windows 安装包（推荐）——自动注册 `.md` / `.markdown` / `.txt` 文件关联 |
| `LightInk-1.0.10-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

**Full Changelog**: v1.0.9...v1.0.10
