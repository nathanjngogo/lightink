# LightInk 轻墨 v1.0.7

修复更新。

## 修复

- **书架 · 重命名文件夹弹窗的「取消」按钮无反应**——现已正常：点击取消（或按 `Esc`）关闭弹窗，文件夹名称保持不变
- 波及面说明：该弹窗组件同样用于「新建文件夹」等文本输入场景，取消按钮一并修复

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.7.exe` | Windows 安装包（推荐）——自动注册 `.md` / `.markdown` / `.txt` 文件关联 |
| `LightInk-1.0.7-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

**Full Changelog**: v1.0.6...v1.0.7
