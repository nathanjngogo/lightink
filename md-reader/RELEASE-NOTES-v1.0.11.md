# LightInk 轻墨 v1.0.11

保存体验优化。

## 优化

- **新建文档弹窗只保留文件命名**——保存地址不在新建时出现，命名更清爽
- **关闭文档时的保存对话框显示完整文件夹路径**
  - 默认为 Windows「文档」文件夹的**真实完整路径**（如 `C:\Users\你\Documents`），不再显示「文档」短词；从已有文件新建则默认其原目录
  - **保存地址改为可编辑输入框**：可以直接手动修改路径，也可以点右侧 📁 按钮浏览选择文件夹
  - 目标文件夹不存在时自动创建；路径首尾多余的引号 / 斜杠自动容错

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.11.exe` | Windows 安装包（推荐）——自动注册 `.md` / `.markdown` / `.txt` 文件关联 |
| `LightInk-1.0.11-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

**Full Changelog**: v1.0.10...v1.0.11
