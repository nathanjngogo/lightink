# LightInk 轻墨 v1.0.0

首个公开版本。本地 Markdown 编辑器 + 阅读器（Typora 形态），离线可用，数据不出本机。

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.0.exe` | Windows 安装包（推荐，88MB）——双击安装，自动注册 `.md` 文件关联 |
| `LightInk-1.0.0-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

> 安装包未做数字签名，SmartScreen 提示时选「更多信息 → 仍要运行」。

## 新特性

- **三种模式**：阅读（默认打开即读）→ 编辑（所见即所得）→ 源码，一键切换
- **自动保存**：停止输入 0.9 秒自动写回原文件，`Ctrl+S` 随时可用
- **阅读位置记忆**：重开文件自动回到上次读到的位置
- **文内查找**：`Ctrl+F`，即时高亮 + 计数 + 循环导航
- **导出**：HTML（自包含单文件，公式字体与图片全内联）/ PDF（A4）
- **书架与最近文件**：固定文库目录 + 最近 12 条，启动自动恢复
- **中文排版**：字号 / 行距 / 版心实时可调
- **完整语法**：GFM 全要素 + KaTeX 公式 + Mermaid 图表 + 代码高亮
- **毛玻璃 UI**：亮 / 暗 / 跟随系统三态主题

## 已知限制

- 安装包未签名（后续版本计划接入签名）
- 导出 PDF 在极长文档（>100 页）上建议先切阅读模式
- 暂不支持多标签页与自定义 CSS（规划中）

**Full Changelog**: 首次发布
