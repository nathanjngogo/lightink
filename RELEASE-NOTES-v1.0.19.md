# LightInk 轻墨 v1.0.19

界面改版 + 两处数据安全修复。

## 界面改版

- **工具栏重排**：右侧集中为「阅读优先 / 保持编辑」开关 → 「自动保存」开关 → 搜索命令框 → 主题 → 导出 → 设置
  - 「自动保存」开关从设置抽屉搬到工具栏，与设置里那一项**双向联动**
  - 主题按钮改为「图标 + 当前主题」，设置按钮改为纯图标
  - 工具栏「新建」按钮移至侧栏底部
- **侧栏**：新增顶部搜索（图标展开，实时过滤两个列表）与「新建分类文件夹」图标；内容分为「最近打开 (N)」与「我的书架 (N)」两组；底部新增通栏「+ 新建文档」按钮
- **欢迎页**：大图标 + 标题 + 副标题 + 版本 /「本地优先」标签居中；「最近打开」增加**相对时间**（刚刚 / N 分钟前 / N 小时前 / 昨天 / N 天前）；「全部清除」改为「清除记录」；拖拽提示改为一行文案
- **状态栏**：左侧显示当前模式与字数，右侧显示「N 个文档 · UTF-8 · 行尾 · 保存状态」（行尾跟随当前文档自动识别 LF / CRLF）

## 修复

- **打开文件不再触发写入**：此前打开任意文档约 1 秒后会产生一次自动保存（状态栏出现「已保存到文件」），即使你什么都没改。现在只有真实编辑才会触发保存。
- **行尾不再被改写**：配合上一条，此前 CRLF（Windows 换行）文件被打开后会被静默改写成 LF。现在写盘时会按原文件行尾还原 —— CRLF 文件保存后仍是 CRLF，LF 文件仍是 LF。

## macOS 版

`lightink-macos/` 源码已同步全部改动（含平台适配块）。

---

**完整更新**：UI 改版（工具栏右簇 / 侧栏分组 / 欢迎页 / 状态栏）+ 打开不写盘 + 行尾保持