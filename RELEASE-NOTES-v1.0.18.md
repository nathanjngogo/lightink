# LightInk 轻墨 v1.0.18

修复多文件编辑场景下的内容丢失问题。

## 新增

- **切换文档前保存提示**：当前文档有未保存修改时，打开另一个文件（或新建文档）会先弹出三选项对话框，不再直接替换内容：
  - **取消** —— 留在当前文档，继续编辑
  - **不保存** —— 放弃当前更改，打开新文档
  - **保存** —— 先保存当前文档再打开新文档（已有文件直接覆盖原文件；新建文档保存到「文档」目录，重名自动加序号）

## 覆盖的入口

打开文件、书架文件、最近文件、拖拽文件、双击关联文件、新建文档 —— 全部接入保存守卫生效。

## 判定逻辑

- 有未保存修改时才会提示；已自动保存到磁盘的文件切换时不打扰
- 配合本次修复：自动保存成功后正确清除「已修改」标记（此前该标记会一直亮着，属于旧版遗留问题）
- 关闭「自动保存」使用时，切换文档一定会提示，不会静默丢失内容

## 修复

- 自动保存成功后，「已修改」圆点与状态栏现在会正确复位

## macOS 版

`lightink-macos/` 源码已同步全部改动。

---

**完整更新**：切换文档不再静默丢失未保存内容（取消 / 不保存 / 保存 三选项）
