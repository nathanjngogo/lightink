# LightInk 轻墨 v1.0.6

常驻后台与关闭行为更新。

## 新增

- **关闭弹窗**：点击右上角 ✕ 关闭软件时弹出选择对话框
  - **最小化到托盘**——窗口收起，LightInk 常驻系统托盘（后台运行，双击托盘图标即可唤回）
  - **退出**——彻底退出程序
  - **取消**——留在当前窗口
  - 可勾选「记住我的选择，下次不再询问」，下次关闭直接生效（之后可在系统托盘菜单退出，或删除 `%APPDATA%\LightInk\settings.json` 中的 `closeAction` 恢复询问）
- **系统托盘**：托盘图标右键菜单「显示主窗口 / 退出 LightInk」，双击图标唤回窗口

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.6.exe` | Windows 安装包（推荐）——自动注册 `.md` / `.markdown` / `.txt` 文件关联 |
| `LightInk-1.0.6-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

**Full Changelog**: v1.0.5...v1.0.6
