# LightInk 轻墨 v1.0.15

A4 纸面宽度自适应更新。

## 新增

- **A4 纸面宽度随内容自适应**：向新建文档粘贴带有格式的宽内容（宽表格、超宽代码块、原始尺寸大图）时，纸面**自动加宽**以完整容纳内容，不再截断或挤压；删除宽内容后纸面**自动缩回**标准 A4 宽度。
  - 基准宽度仍为 A4（794px），加宽上限 1400px，防止过宽溢出窗口
  - 表格按内容撑宽：窄表格仍占满纸面，宽表格按实际列宽展开
  - 超出加宽上限的元素在纸内横向滚动兜底

## 下载

| 文件 | 说明 |
|---|---|
| `LightInk-Setup-1.0.15.exe` | Windows 安装包（推荐）——自动注册 `.md` / `.markdown` / `.txt` 文件关联 |
| `LightInk-1.0.15-portable.zip` | 免安装便携版——解压即用 |

系统要求：Windows 10 / 11（64 位）。

**Full Changelog**: v1.0.14...v1.0.15
